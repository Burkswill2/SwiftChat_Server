import XCTest
@testable import SwiftChat_Server

final class SwiftChat_ServerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftChat_Server().text, "Hello, World!")
    }
}
